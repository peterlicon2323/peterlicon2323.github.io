<div class="list-groups row">
  {%- comment -%}
    `data` is the array stored under `additional_information` in
    `_data/cv.json`. Each element is an object with a single key/value
    pair, e.g.:

    { "certificate details": "https://google.com" }
  {%- endcomment -%}

  {%- for entry in data -%}
    {%- assign label = entry | keys | first -%}
    {%- assign url   = entry[label] -%}

    <div class="list-group col-md-6 mb-3">
      <table class="table-cv list-group-table">
        <tbody>
          <tr>
            <td class="list-group-category-icon">
              <i class="fa-solid fa-link"></i>
            </td>
            <td class="list-group-category">{{ label }}</td>
          </tr>
          <tr>
            <td></td>
            <td class="list-group-name">
              <a href="{{ url }}" target="_blank" rel="noopener">
                {{ url }}
              </a>
            </td>
          </tr>
        </tbody>
      </table>
    </div>
  {%- endfor -%}
</div>
